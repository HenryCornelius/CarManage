{% extends 'base.html.twig' %}

{% block title %}Modifier le véhicule{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier mon véhicule</h1>

        {{ include('car/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('car/_delete_form.html.twig') }}

        <a class="btn btn-secondary mt-2" href="{{ path('app_car_index') }}">Retour à ma liste</a>
    </div>
    
{% endblock %}
